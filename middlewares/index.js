const validateCreateCarInfo = require('./validateCreateCarInfo');
const validateDriverName = require('./validateDriverName');
const validateDriverId = require('./validateDriverId');
const validateRegistryInfo = require('./validateRegistryInfo');
const validateUpdateRegistry = require('./validateUpdateRegistry');

module.exports = {
  validateCreateCarInfo,
  validateDriverName,
  validateDriverId,
  validateRegistryInfo,
  validateUpdateRegistry,
};
